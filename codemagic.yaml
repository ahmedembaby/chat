workflows:
  expo-multi-platform:
    name: Expo iOS & Android
    environment:
      vars:
        EXPO_TOKEN: $EXPO_TOKEN
      node: 18.16.0
    scripts:
      - npm install -g eas-cli
      - npm install
      - eas build --platform all --non-interactive --token $EXPO_TOKEN
    artifacts:
      - build/**/*.apk
      - build/**/*.aab
      - build/**/*.ipa
